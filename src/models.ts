export interface Product {
    id: number
    name: string
    price: number
    size: string //S M L
    image: string
  }

export interface User {
    id: number
    email: string
    password: string
}